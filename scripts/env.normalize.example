# Copy this file to: scripts/.env.normalize
# (That path is gitignored — never commit real keys.)

SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
# Publishable (anon) key is enough if your Storage policies allow it; otherwise use service_role.
SUPABASE_SERVICE_ROLE_KEY=your_key_here

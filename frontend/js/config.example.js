/**
 * Copy this file to config.js and fill in values from the Supabase dashboard.
 * NEVER commit config.js with real keys if the repo is public (anon key is public-safe but still keep habits).
 *
 * Only the anon key goes here. OpenAI + ElevenLabs keys belong ONLY in Supabase Edge Function secrets.
 */
window.NIHONGO_CONFIG = {
  SUPABASE_URL: "https://YOUR_PROJECT_REF.supabase.co",
  SUPABASE_ANON_KEY: "YOUR_SUPABASE_ANON_KEY",
};
